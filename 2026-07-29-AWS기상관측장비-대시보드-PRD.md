# 📋 PRD — AWS 기상관측장비 관리 웹 대시보드

> 작성일: 2026-07-29
> 개정: 2026-07-30 (v0.3 — 점검일·상태 관리 제외)
> 상태: 개발 착수 가능 (⚠️ 표시 항목만 확인 필요)

## 1. 목적 (한 문장)
광주·전남에 흩어진 AWS(자동기상관측장비)의 위치·현장환경을 **모바일 한 화면에서 확인하고, 현장까지 가장 빠르게 찾아가도록 돕는** 신뢰성 있는 대시보드입니다.

> 대상 지역: **광주광역시 · 전라남도** (2026-07-29 확정)

## 2. 핵심 사용자
- **주 사용자 — 기상청 관측 담당 직원**: 현장 점검·수리를 위해 출장을 나가는 사람. "어디에, 어떻게 가고, 현장은 어떤 상태인지"가 중요합니다.
- **보조 사용자 — 일반인/방문객**: 우리 지역 관측장비가 어디 있고 어떤 곳인지 궁금한 사람. 신뢰감 있는 소개용.

## 3. 페이지 구성 (섹션 순서 — 위→아래 스크롤)
모바일 랜딩페이지 기준, 한 손으로 위아래 스크롤하며 보는 구조입니다.

1. 상단 헤더 (로고 + 한 줄 소개)
2. 지도 + 장비 목록
3. 길찾기 / 최적 경로
4. 현장 VR 둘러보기
5. 하단 정보 (문의·기관 정보)

> 초안에 있던 **핵심 요약 카드(오늘 한눈에)** 는 2026-07-29 제외했습니다. 요약할 상태 정보를 두지 않기로 했고(6절), 지도를 먼저 보여주는 편이 낫다고 판단했습니다.

> 장비 **상세는 별도 페이지가 아니라 바텀시트**(화면 아래에서 올라오는 카드)입니다. 페이지 이동 없이 지도 위에서 바로 확인합니다.

## 4. 각 섹션 내용

**① 상단 헤더** — 기관 로고, "광주·전남 AWS 기상관측망 관리" 타이틀 한 줄.

**② 지도 + 장비 목록** — 지도(OpenStreetMap)에 장비 위치 핀 표시. 핀이나 목록 항목을 누르면 **바텀시트**로 상세가 올라오고, 지도도 해당 위치로 이동합니다.
- **장비 목록은 기본으로 접혀 있습니다.** 제목 줄(`관측장비 목록 N곳`)을 눌러 펼칩니다. 지도를 먼저 보게 하고 화면을 짧게 유지하기 위함입니다.
- **검색 방식 3종**: `전체 검색`(지점명·주소·번호) / `주소 검색` / `관측장비번호`. 검색어를 입력하면 접힌 목록이 자동으로 펼쳐집니다.
- 지도에는 `전체 보기` 버튼을 두어 확대·축소 후 처음 범위로 돌아올 수 있게 합니다.

**바텀시트 상세 내용** — 지점명/지점번호, 주소, 좌표, 비고, 그리고 버튼 2개: `길안내` / `현장 둘러보기`.

**③ 길찾기 / 최적 경로**
- **단일 안내**: 바텀시트의 `길안내` 버튼 → 카카오맵/네이버지도 앱 네비게이션으로 넘김.
- **하루 출장 경로**: 장비를 여러 개 선택 → **사무실 출발 → 선택 장비 전부 → 사무실 복귀(왕복)** 기준으로 총거리가 가장 짧은 순서를 계산해 표시합니다.
  - 출발·도착지: **사무실 고정** (좌표는 설정값으로 관리)
  - 선택 가능 개수: **최대 8곳**
  - 거리 기준: **직선거리 근사**(위경도 기반). 실제 도로 거리가 아니므로 화면에 "대략적인 순서 안내입니다"를 명시합니다.
  - 결과: 방문 순서 목록 + 구간별 거리 + 총 거리

**④ 현장 VR 둘러보기** — 장비 주변 360° 환경을 확인. 나무·건물 등 관측 방해 요소를 현장에 가지 않고 미리 점검.
- 형식: **360° 파노라마 사진 (equirectangular, 가로:세로 = 2:1, JPG, 가로 4096px 이상 권장)**
- 파일 위치: `panoramas/` 폴더, 파일명은 CSV의 `panorama` 칸으로 연결
- 사진이 없는 장비는 "촬영 예정" 안내를 표시합니다.

**⑤ 하단 정보** — 담당 부서·연락처, **장비 데이터 기준일**(CSV 갱신일, 페이지 배포일이 아님).

## 5. 데이터 (CSV)

장비 정보는 **CSV 파일 1개**로 관리합니다. 담당자가 엑셀에서 수정 후 파일만 교체하면 반영됩니다. (별도 관리자 화면 없음)

**파일**: `data/stations.csv` (인코딩 UTF-8)

| 칸 이름 | 뜻 | 예시 | 필수 |
|---|---|---|---|
| `station_id` | 지점번호(관측장비번호) | 165 | ✅ |
| `name` | 지점명 | 목포 | ✅ |
| `lat` | 위도 | 34.8168 | ✅ |
| `lon` | 경도 | 126.3812 | ✅ |
| `address` | 주소 | 전남 목포시 연산동 | ✅ |
| `panorama` | 파노라마 파일명 (없으면 빈칸) | 165.jpg | — |
| `note` | 비고 | 주변 수목 성장 | — |

## 6. 상태 판정 규칙 — 2026-07-30 제외

v0.2에는 `last_check + cycle_months`로 **정상 / 곧 도래 / 점검필요**를 자동 계산해 목록 배지·지도 핀 색·상태 필터로 보여주는 규칙이 있었습니다. 이 기능은 **전부 제외했습니다.**

**제외 사유** — 점검 이력은 이미 별도 업무 체계에서 관리하고 있어, 대시보드에서 이중으로 관리하면 두 값이 어긋날 때 오히려 신뢰를 떨어뜨립니다. 이 대시보드는 **"어디에 있고, 어떻게 가고, 현장이 어떤지"** 에 집중합니다.

**함께 없어지는 것**

- CSV의 `last_check` · `cycle_months` 칸 (지상·해양 양쪽)
- 목록 상태 배지, 상태 필터(`전체 / 점검필요 / 곧 도래 / 정상`)
- 바텀시트의 마지막 점검일 · 점검 주기 · 다음 점검 예정일 3줄

**대신** — 현장에서 챙길 사항은 `note`(비고) 칸에 자유롭게 적습니다.

> 지도 핀은 원래도 상태 색을 쓰지 않고 선택 여부만 구분하므로 바뀌지 않습니다.

## 7. 디자인 방향
- **톤**: 신뢰감 (기상청 블루/네이비 + 화이트, 절제된 색)
- **레이아웃**: 모바일 우선, 한 화면에 핵심이 보이는 카드형
- **가독성**: 큰 글씨, 충분한 대비, 버튼은 크고 누르기 쉽게
- **접근성**: 정보를 **색만으로 구분하지 않습니다.** 색을 쓸 때는 글자나 아이콘을 항상 함께 둡니다.
- **속도감**: 무겁지 않게, 필요한 정보 먼저

## 8. 예외 상황 처리
- **위치 권한 거부/실패**: 길안내는 그대로 동작(외부 앱이 처리). 최적 경로는 사무실 고정 출발이므로 현재 위치 없이도 계산됩니다.
- **지도 로딩 실패**: 지도 자리에 안내 문구를 띄우고, 아래 장비 목록은 정상 동작합니다.
- **좌표가 비었거나 형식이 틀림**: 해당 장비는 지도 핀을 찍지 않고 좌표를 "확인 필요"로 표시하며, 길안내·경로 담기를 비활성화합니다. 목록에는 그대로 나오고 나머지 장비는 정상 동작합니다.
- **`station_id`·`name`이 둘 다 빈 줄**: 읽지 않고 건너뜁니다.

## 9. 이번엔 안 만들 것 (범위 밖)
- 실시간 기상 데이터 연동(온도·강수 실측값 스트리밍) → 이번엔 장비 **관리** 중심
- 로그인·권한 관리 시스템
- 웹에서 장비 데이터 입력·수정 (CSV 파일 교체로 대체)
- 실제 VR 촬영본 제작 (자리와 규격만 마련)
- 실제 도로 거리 기반 경로 계산 (직선거리 근사로 대체)
- PC 전용 화면 최적화 (모바일 먼저)
- 오프라인 동작
- 핵심 요약 카드(오늘 한눈에) — 2026-07-29 제외 결정
- **점검 이력·점검 주기 관리와 상태 자동 판정** — 2026-07-30 제외 결정 (6절)

## 10. ⚠️ 아직 확인이 필요한 것

개발을 시작할 수는 있지만, 아래는 완성 전에 답이 필요합니다.

0. ~~**실제 지점 목록**~~ — **해결됐습니다.** 실제 지점표 100개소(지점번호·지점명·주소·좌표)로 교체 완료.
1. **사무실 좌표** — 최적 경로의 출발·도착지. 주소만 주셔도 됩니다.
2. **좌표 공개 범위** — 로그인이 없으므로 광주·전남 전 장비의 정확한 좌표가 누구에게나 공개됩니다. 그대로 괜찮은지 확인이 필요합니다.
3. **배포 위치** — 기관 홈페이지 / 사내망 / 별도 주소 중 어디인지. 지도와 위치 기능은 **HTTPS**에서만 정상 동작합니다.
4. **담당 부서·연락처** — ⑤ 하단 정보에 넣을 실제 값.

---

**1차 버전 진행 상황** — 샘플 CSV(광주·전남 15곳)로 ①~⑤ 화면과 흐름을 **완성했습니다**(2026-07-29). 실제 지점표를 받아 CSV만 교체하면 됩니다.

**v0.3 반영 상황**(2026-07-30) — CSV와 문서는 정리했으나 `index.html`은 아직 v0.2 코드 그대로입니다. 지금 화면을 열면 전 지점이 "정보 확인 필요"로 표시되며, `README.md`의 "상태 기능 걷어내기" 목록대로 수정해야 합니다.
